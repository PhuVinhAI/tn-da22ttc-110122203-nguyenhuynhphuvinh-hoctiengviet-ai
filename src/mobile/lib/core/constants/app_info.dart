/// Single source of truth for app identity strings displayed in UI.
/// Keep [appVersion] in sync with the `version:` field in `pubspec.yaml`.
library;

const String appName = 'LinVNix';
const String appVersion = '0.0.03062026';
const String appTagline = 'Learn Vietnamese, your way';
